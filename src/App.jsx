import React from "react";
import ReactDOM from "react-dom/client";
import TodoList from "./public/main";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TodoList />
  </React.StrictMode>
);
